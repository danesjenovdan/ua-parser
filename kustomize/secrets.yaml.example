apiVersion: v1
kind: Secret
metadata:
  name: parlaparser-ukraine-credentials
  labels:
    app.kubernetes.io/name: parlaparser-ukraine-credentials
    app: parlaparser-ukraine-credentials
stringData:
  PARSER_USER: "user"
  PARSER_PASSWORD: "password"
  API_URL: "http://localhost:8000/v3"
